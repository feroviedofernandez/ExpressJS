{
  "name": "middleware",
  "version": "1.0.0",
  "description": "Las funciones de middleware son funciones que tienen acceso al objeto de solicitud (req), al objeto de respuesta (res) y a la siguiente \r función de middleware en el ciclo de solicitud/respuestas de la aplicación.<br />\r La siguiente función de middleware se denota normalmente con una variable denominada next.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "feroviedofernandez",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
